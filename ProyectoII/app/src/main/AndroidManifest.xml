<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoii">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VerPublicacionActivity"
            android:theme="@style/ActivityWithToolbar">

        </activity>
        <activity
            android:name=".BeforeLogin.ExtraInfoActivity"
            android:theme="@style/ActivityWithToolbar" />
        <activity
            android:name=".Post"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/ActivityWithToolbar"/>
        <activity
            android:name=".BeforeLogin.RegisterActivity"
            android:theme="@style/ActivityWithToolbar" />
        <activity
            android:name=".BeforeLogin.LogInActivity"
            android:theme="@style/LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>